# Trigger evaluation fixtures

`trigger-cases.json` separates API/relay requests from ordinary built-in image requests. Run these cases through the target agent's skill-selection evaluator after changing the frontmatter description.

The package validator checks fixture structure and coverage. It does not simulate a proprietary host router.
